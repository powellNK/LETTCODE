class Solution {
    public int[] getConcatenation(int[] nums) {
        int lenghtNums = nums.length;
        int[]  ans = new int[2*lenghtNums];
        for (int i = 0; i<nums.length;i++){
            ans[i]=ans[i+lenghtNums]=nums[i];
        }
        return ans;
    }
}